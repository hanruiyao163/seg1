# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: D:/books-dataset/yolo-exp-etl-data # dataset root dir (absolute or relative; if relative, it's relative to default datasets_dir)
train: test/output-yolo # train images (relative to 'path') 4 images
val: test/output-yolo # val images (relative to 'path') 4 images
test: test/output-yolo # test images (optional)

# Classes (80 COCO classes)
names:
    0: Book
    1: Inkiness
    2: Vitium
    3: Crease
    4: defaced
    5: Patch
    6: Signature